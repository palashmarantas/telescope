---
framework: jest
preRun:
     command:
        - npm ci
preMerge:
  pattern:
    - "./src/core/utils/*.js"
postMerge:
  pattern: 
    - "./src/core/utils/*.js"
postRun:
     command:
        - node --version
version: 1.0
